# FrogNet Dashboard v2 (Fawcett Innovations, LLC)

### What's new
- Header title **centered** and at **2×** size.
- FrogNet hero logo now **1.5×** (90px tall).
- **Sensors & Actuators** show their **Type in bold**.
- **Teams**: "Create Team" button and per-team **"Add Teammate"** form.
- **WellKnownSites**: SiteAddress is clickable when it looks like a URL, plus **Add WellKnownSite** form.

Place these files with images:
- `FI_Logo.png`, `FN_Logo.png`, `DisasterComm.png`

Set your API URL in the top-right input. The API must respond with `{ ok: true, rows: [...] }` format.
